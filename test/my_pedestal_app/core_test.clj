(ns my-pedestal-app.core-test
  (:require [clojure.test :refer [deftest is testing]]
            [my-pedestal-app.core :as core]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 (core/foo 1)))))
